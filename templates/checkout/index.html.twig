{% extends 'base.html.twig' %}
{% block body %}
    <div class="mx-auto max-w-3xl px-4 pt-28 pb-16">
        <h1 class="boldonse-regular text-3xl text-gray-900 mb-8">Checkout</h1>
        <p class="text-gray-500">Checkout is being set up. Your cart total is
            <span class="font-semibold text-gray-900">{{ cart.totalInGbp|format_currency('GBP') }}</span>.</p>
    </div>
{% endblock %}
